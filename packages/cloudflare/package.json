{
  "name": "announcing-cloudflare",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --local",
    "start": "wrangler dev",
    "build": "wrangler publish --tsconfig tsconfig.build.json --dry-run --outdir dist",
    "deploy": "wrangler publish --tsconfig tsconfig.build.json --minify",
    "test": "jest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^3.14.1",
    "jest-environment-miniflare": "^2.7.1",
    "wrangler": "^2.0.27"
  },
  "dependencies": {
    "@announcing/shared": "workspace:*",
    "jose": "^4.9.2"
  }
}
