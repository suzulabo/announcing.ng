{
  "name": "@announcing/shared",
  "private": true,
  "files": [
    "dist"
  ],
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsc -p tsconfig.build.json",
    "test": "jest",
    "lint": "eslint src && prettier --check src"
  }
}
